[pytest]
testpaths = tests
pythonpath = .
addopts = -ra --strict-markers --tb=short
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: hits real Supabase or network (not run in default CI)
    api: HTTP API tests (mocked external services)
